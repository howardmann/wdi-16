<h1>Make a pledge for <%= @project.name %>:</h1>

<%= form_for([@project, @pledge]) do |f| %>
  <p>
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </p>
  <p>
    <%= f.submit %>
  </p>

<% end %>
