<h1>Edit pledge for <%= @pledge.project.name %>:</h1>

<%= form_for([@user,@pledge]) do |f| %>
  <p>
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </p>
  <p>
    <%= f.submit %>
  </p>

<% end %>
